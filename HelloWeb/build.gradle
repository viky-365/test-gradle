apply plugin: 'war'
apply plugin: 'jetty'

repositories {
    mavenCentral()
}

dependencies {
    compile "javax.servlet:servlet-api:3.+"
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

httpPort = 8080
stopPort = 9451
stopKey = 'foo'